<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title 
    body.body
        .wrapper
            //- include components/textForm/dropDown/dropDown.pug 
            //- include components/likeButton/likeButton.pug
            //- +likeButton()
            include components/rateStar/rateStar.pug
            +Star({isStar: false})